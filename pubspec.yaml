name: grocery_app_demo
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  velocity_x: ^4.1.1
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  animated_splash_screen: ^1.3.0
  photo_view: ^0.14.0
  country_picker: ^2.0.21
  intl_phone_field: ^3.2.0
  csc_picker: ^0.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/

